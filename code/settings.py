import pygame
import os
from os.path import join
from os import walk

CODE_DIRECTORY = os.path.dirname(os.path.abspath(__file__))
ROOT_DIRECTORY = os.path.dirname(CODE_DIRECTORY)

WINDOW_WIDTH, WINDOW_HEIGHT = 1280,720
TILE_SIZE = 64
